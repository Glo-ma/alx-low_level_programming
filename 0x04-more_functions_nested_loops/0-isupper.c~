#include "holberton.h"

/**
 * _isupper: returns 1 for a uppercase letter
 * @c: function prototype for isupper
 * return: (1) if uppercase
 * 
 */

int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')

	{
		return (1);
	}
else
	{
		return (0);
	}
}
